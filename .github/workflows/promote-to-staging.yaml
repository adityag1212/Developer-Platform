name: Promote to Staging

on:
  workflow_dispatch:
    inputs:
      image_tag:
        description: "Image SHA to promote"
        required: true
        type: string

permissions:
  contents: write

jobs:
  promote:
    name: Promote Image to Staging
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: develop
          fetch-depth: 0

      - name: Update staging image
        env:
          IMAGE_TAG: ${{ inputs.image_tag }}
        run: |
          sed -i \
            "s/tag: \"[^\"]*\"/tag: \"${IMAGE_TAG}\"/" \
            gitops/applications/demo-app/values-staging.yaml

      - name: Commit promotion
        env:
          IMAGE_TAG: ${{ inputs.image_tag }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add gitops/applications/demo-app/values-staging.yaml

          git diff --cached --quiet && exit 0

          git commit -m "chore: promote demo-service ${IMAGE_TAG} to staging"
          git push origin develop